# devenv integration: pulls in Go, Postgres, sqlc, goose, ...
eval "$(devenv direnvrc)"
use devenv

# Export DB_URL / JWT_SECRET / PLATFORM so CLI tools (goose, psql) work
# without sourcing .env by hand. The app itself still loads .env via godotenv.
dotenv_if_exists .env
